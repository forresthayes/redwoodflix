# Migration `20200522180638-create-movie-and-review`

This migration has been generated by Forrest Hayes at 5/22/2020, 6:06:38 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."Movie" (
"description" text  NOT NULL ,"director" text  NOT NULL ,"duration" text  NOT NULL ,"id" SERIAL,"image_file_name" text  NOT NULL ,"rating" text  NOT NULL ,"released_on" timestamp(3)  NOT NULL ,"title" text  NOT NULL ,"total_gross" integer  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."Review" (
"comment" text  NOT NULL ,"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"id" SERIAL,"movieId" integer  NOT NULL ,"name" text  NOT NULL ,"stars" integer  NOT NULL ,
    PRIMARY KEY ("id"))

ALTER TABLE "public"."Review" ADD FOREIGN KEY ("movieId")REFERENCES "public"."Movie"("id") ON DELETE CASCADE  ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200522180638-create-movie-and-review
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,34 @@
+datasource DS {
+  provider = "postgresql"
+  url      = env("DATABASE_URL")
+}
+
+generator client {
+  provider      = "prisma-client-js"
+  binaryTargets = env("BINARY_TARGET")
+}
+
+// Define your own datamodels here and run `yarn redwood db save` to create
+// migrations for them.
+model Movie {
+  id              Int      @default(autoincrement()) @id
+  title           String
+  description     String
+  released_on     DateTime
+  rating          String
+  total_gross     Int
+  director        String
+  duration        String
+  image_file_name String
+  reviews         Review[]
+}
+
+model Review {
+  id        Int      @default(autoincrement()) @id
+  name      String
+  stars     Int
+  comment   String
+  createdAt DateTime @default(now())
+  movie     Movie    @relation(fields: [movieId], references: [id])
+  movieId   Int
+}
```


